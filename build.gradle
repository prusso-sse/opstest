apply plugin: 'java'
apply plugin: 'eclipse'

version = '1.0'
sourceCompatibility = 1.7
targetCompatibility = 1.7

jar {
    manifest { 
        attributes('Main-Class': 'com.aol.opstest.OpsTest') 
    }
}

// Maven central
repositories {
    mavenCentral()
}

// Dependencies
dependencies {
    testCompile 'junit:junit:4.12'
}